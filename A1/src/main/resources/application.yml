spring:
  application:
    name: A1
  cloud:
    nacos:
      config:
        import-check:
          enabled: false
      discovery:
        username: nacos
        password: nacos
        server-addr: localhost:8848
        namespace:

server:
  port: 8002
